[project]
name = "italia"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "curl-cffi>=0.14.0",
    "pandas>=3.0.1",
    "pymongo>=4.16.0",
    "python-dotenv>=1.2.2",
]
